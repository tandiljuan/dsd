# services:
#   dagger:
#     image: dagger/dagger:latest
#     volumes:
#       - /var/run/docker.sock:/var/run/docker.sock
#
# ----------
#
# import docker
# client = docker.DockerClient(base_url='unix:///var/run/docker.sock')
# client.services.get('my-service').update(image='my-image:latest')
